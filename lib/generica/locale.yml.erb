# File generated by yaml_writer
# <%= translations.count %> translations
<%= locale.inspect %>:
<% translations.each do |translation| %>
  <%= translation.key.inspect %>: <%= translation.translated_text.inspect %>
<% end %>